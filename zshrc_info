# ZSHRC profile example

# NOTE: brew warns to use libunwind and libc++ provided by macOS rather than the homebrew version as this can cause problems
# commented out LLVM path currently due to above warnings. 
# Using flags from homebrew LLVM rather than system LLVM may result in issues. These paths may require changing to local LLVM installs
# export PATH="/opt/homebrew/opt/llvm/bin:$PATH"
export LDFLAGS="-L/opt/homebrew/opt/libomp/lib"
export LDFLAGS="$LDFLAGS -L/opt/homebrew/opt/llvm/lib"
export CPPFLAGS="-I/opt/homebrew/opt/libomp/include"
export CPPFLAGS="$CPPFLAGS -I/opt/homebrew/opt/llvm/include"

# python
export PATH="/usr/local/opt/python/libexec/bin:$PATH"

# pyenv
if command -v pyenv 1>/dev/null 2>&1; then
    eval "$(pyenv init -)"
fi

# AD root directory
export ACMACSD_ROOT="/Users/${USER}/Desktop/pipeline/AD"
